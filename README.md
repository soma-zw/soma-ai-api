# soma-ai-api
Shared AI backend for the Soma browser's two assistants:

- **`page`** — the sidebar assistant, grounded strictly in the currently open webpage's content.
- **`assistant`** — the home chat assistant, general-purpose and context-aware (student profile, conversation history, eventually Supabase-backed data).

Built with FastAPI + Groq, deployed on Render.
